APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

################################################################################
# MAP SSO Configuration
################################################################################

# MAP Base URL - Main URL of the MAP application
# Local: http://127.0.0.1:8000
# Production: https://map.muamalat.com.my
MAP_BASE_URL=http://127.0.0.1:8000

# MAP SSO Shared Secret - MUST match the secret in MAP application
# IMPORTANT: Change this in production and keep it secure!
# This secret is used for HMAC token signing/verification
MAP_SSO_SECRET=map-eform-sso-shared-secret-2024

# SSO Token Expiry (in seconds)
# Default: 60 seconds (1 minute)
# Increase if users experience token expiration issues
MAP_SSO_TOKEN_EXPIRY=60

# MAP Redirect URL - Where MAP redirects users after login
# Local: http://127.0.0.1:8000/redirect/eform/
# Production: https://map.muamalat.com.my/redirect/eform/
MAP_REDIRECT_URL=http://127.0.0.1:8000/redirect/eform/

# MAP Logout URL - For federated logout (logging out of eForm also logs out of MAP)
# Local: http://127.0.0.1:8000/pengurusan/logout/
# Production: https://map.muamalat.com.my/pengurusan/logout/
MAP_LOGOUT_URL=http://127.0.0.1:8000/pengurusan/logout/

# MAP API Verify Endpoint - For production SSO verification
# Production: https://map.muamalat.com.my/api/eform/verify/
MAP_VERIFY_URL=https://map.muamalat.com.my/api/eform/verify/

################################################################################
# MAP Database Configuration (for data sync)
################################################################################

# MAP Database Path for Sync Operations
# This is the path to MAP's SQLite database file
# 
# Local development: 
#   ../FinancingApp/FinancingApp_Backend/FinancingApp/db.sqlite3
# 
# Production examples:
#   Docker: /map_db/db.sqlite3
#   Direct: /opt/FinancingApp/FinancingApp_Backend/FinancingApp/db.sqlite3
#   Custom: /path/to/your/MAP/db.sqlite3
MAP_DATABASE_PATH=../FinancingApp/FinancingApp_Backend/FinancingApp/db.sqlite3

# eForm Database Path (for Docker volume mounting)
# Production: /opt/eform/eform_db
# Local: ./database
MAP_DB_PATH=./database
